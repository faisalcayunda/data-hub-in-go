services:
  api:
    image: portal-data-backend:latest
    container_name: portal-data-api
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env.runtime
    pull_policy: never
    command: gunicorn -c gunicorn.conf.py app:app
